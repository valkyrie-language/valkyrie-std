# Atlas workspace

Atlas 子包位于 `projects/atlas/projects/*`，workspace 清单为本目录下的 `legions.von`。

发行版入口（嵌套 legions）：

```bash
cd valkyrie.v/atlas.v
legion build
```

全量集成构建：

```bash
cd valkyrie.v
legion build
```
