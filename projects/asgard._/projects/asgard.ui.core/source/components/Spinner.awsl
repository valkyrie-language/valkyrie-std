<widget Spinner>
<svg viewBox="0 0 24 24" :width="px" :height="px" aria-hidden="true" @class="ui-spinner ui-spinner-{variant}">
    <path @class="ui-spinner-path" d="M12 2v4M12 18v4M4.9 4.9l2.8 2.8M16.3 16.3l2.8 2.8M2 12h4M18 12h4M4.9 19.1l2.8-2.8M16.3 7.7l2.8-2.8" />
</svg>
</widget>

<script>
let size: utf8 = "md"
let variant: utf8 = "default"
let mut px: utf8 = "24"

micro onMount() {
    if size == "sm" {
        px = "16"
    } else if (size == "lg") {
    px = "32"
} else {
px = "24"
}
}
</script>
<style>
.ui-spinner {
    display: inline-block;
    vertical-align: middle;
    flex-shrink: 0;
    animation: ui-spinner-rotate 0.8s linear infinite;
}
.ui-spinner-path {
    fill: none;
    stroke: currentColor;
    stroke-width: 2;
    stroke-linecap: round;
    stroke-linejoin: round;
}
.ui-spinner-default { color: var(--sys-on-bg-tertiary); }
.ui-spinner-primary { color: var(--sys-primary); }
@keyframes ui-spinner-rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
</style>
