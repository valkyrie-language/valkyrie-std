# VOA 项目配置 -- Core Components 展示
export let projectConfig = {
    name: "voa-core-showcase",
    version: "0.1.0",
    render: {
        defaultMode: "csr",
        routes: [
            { path: "/", mode: "csr" }
        ]
    }
}
