namespace std.data.text.valkyrie;

# Valkyrie source-text models — isomorphic to Rust `std-data::text::valkyrie`.
#
# Naming:
# - language name = **Valkyrie** (package: `std.data.text.valkyrie`)
# - source file suffix = **`.v`** (not a separate `std.data.text.v` package)
#
# Owns AST / CST / lexer / span models for Valkyrie. Language frontend consumes these.
# Do not reintroduce a parallel `std.data.text.v` crate.
